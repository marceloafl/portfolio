'use client';

import styled from "styled-components";

export const DraggerWrapper = styled.section`
  display: flex;
  gap: 24px;
  overflow-x: scroll;

  a {
    height: 288px;
    min-width: 192px;
    position: relative;
  }
`;
